# 1.15 断链行动与轨道背景：验证记录

## 当前构建

Unity 6000.6.0f1。Mac 构建标识 `f10a7a67217e2041b5d7f7d555729044f64450ac28520f421e76492d57f3514a`；Web 构建标识 `9a86bf4866bd96097257ef43a0626a270bef0da5ddd07545eb8b4a9bbb852bbc`。两个目标的设置不同，构建标识分别核对；1379 个游戏资源与源码文件在两个目标中内容一致。Mac 构建报告为 Succeeded，0 错误、0 警告。

## 游戏流程与机制

- 原空战运行检查 484 项通过，0 失败。覆盖原主线机制、场景、武装、敌机坠落与爆炸、音效、人物通讯及三语录音资源。
- 苍隼、蝠鲼、银针分别完成原空战三章，最终均为 Victory。控制器使用正常玩家装甲、武器、冷却和升级；空战以 2 倍模拟速度运行，未跳过敌方生命或章节时间。实际运行分别约 170、176、169 秒。
- 地面机制检查 49 项通过：三种机甲与三种履带、炮塔瞄准、有限防盾、跃迁与落地解卡、双方弹道共用掩体判定、可破坏掩体、救援连接、首领护盾与弱点、四区推进、章间强化、结局和返回清理。
- 三架机体各完成地面四区，均击破 24 个目标并完成两处救援，使用正常装甲与技能。游戏内时间分别约 66.65、61.50、62.70 秒。自动控制器的路径判断使用真实掩体与敌军占地，包含绕障和切换形态。
- 修复了真实浏览器检查发现的空战配音覆盖地面简报的问题。地面新剧情以字幕呈现，原空战三语录音不会误触发地面简报。

这些是自动运行验证，不能等同于三位真人的主观难度和乐趣评价。完整说明与操作见 [地面战役与轨道背景](地面战役与轨道背景1.15.md)。

## 浏览器与人物呈现

当前正式网页构建完成 182 项功能和布局检查：地面触控 42 项、原空战手机回归 100 项、WebKit 20 项、桌面原生菜单和键盘 12 项、Android 模拟 8 项。

检查包含：390×844、360×640 的竖屏，844×390、667×320 的横屏，新地面入口可达，完整任务文字，人物通讯画面，移动与第二指施放，变形、防盾、跃迁、支援轰击，转屏自动暂停和释放摇杆，暂停重试、返回机库，英文与日语试听，摄像机比例一致、无横向溢出、绘制缓冲区不反复变化。未发现浏览器脚本或渲染错误。

当前 Mac 版本重新采集了三位角色的机库、档案与 16 秒姿态序列。抽看 2.5 秒与 8.75 秒的三角色画面，比例完整，姿态变化存在，未见明显纹理撕裂。角色资源和随动算法未修改；本记录不声称逐帧人工观看。

浏览器环境是 Mac 上的 Chrome 和 WebKit，使用 iPhone / Android 尺寸、像素比和触控模拟。没有实体 iPhone、Android 或微信内置浏览器实测，不能将这些结果当作真实手机性能保证。

## 性能与声音

地面 Mac 自动通关记录的平均帧时间约 17.05–17.09 ms，p95 为 16.88–17.34 ms。记录包含区域加载和截图，最大单帧约 0.60–0.63 秒；区域目前仍为同步生成，记录也包含截图开销；本轮没有逐个归因峰值，不能据此声称所有加载过程无停顿。

在其他游戏测试退出后，对最终网页的横竖屏各采样 10 秒。游戏内部 FPS 读数约 60.09 / 60.02，音乐持续播放。浏览器 requestAnimationFrame 运行于约 120 Hz，其 p95 约 9.1 ms，仅代表浏览器回调调度，不能解释为游戏以 120 FPS 渲染，也不是手机 GPU 帧时间。

原空战密集击破检查：180 次击破、360 帧，平均 17.315 ms，p95 16.842 ms，p99 33.158 ms；最大单次击破处理约 0.127 ms，无新增冲击对象分配。新增地面模式复用弹丸、火花和地表冲击渲染；静态地面设施按材质合并，掩体保留独立碰撞与破坏状态。

原有 444 段录音和音乐文件相对 1.14 没有改动。选机音乐连续，地面根据普通作战、首领和结算切换已有配乐。新增地面对白未录制新音频，不把字幕标为新增三语配音。

## 交付边界

本轮新增 15 件 Blender 资产，可编辑源文件、FBX、Unity 预制体及生成脚本一并保留。地面模式单独记录完成情况、最高分与救援结果，原空战的章节和专精继续使用。

网页沿用同站静态资源、CSP、入口脚本与样式的 SRI、no-referrer。模板及正式构建通过内容篡改、路径越界与资源完整性检查；启动期间未观察到其他站点的资源请求。最终数据文件为 104523701 字节，低于 GitHub 100 MiB 单文件限制。秘密扫描、源包一致性与远端部署结果另保存在本轮发布记录中。

Mac 下载包使用本地签名，未进行 Apple 公证。Windows、实体移动设备和联网功能不在本轮验证范围内。
